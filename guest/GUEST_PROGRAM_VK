# PENDING: derive and record the programVK for the reproducible guest ELF
# with sha256 7cb3289fbd5f99f083d9edcc702b61bcd85b6bc445a155425d3954acfc4d7ec1
# (see GUEST_ELF_SHA256) on a ZiSK prover box with the v0.18.0 proving key:
#
#   cargo-zisk program-setup -e out/zksync-os-zisk-guest \
#       -k ~/.zisk/provingKey
#
# The previous value belonged to the bump-allocator ELF (before the wired
# crypto hooks of plan 6.9) and must not be used with this build.
# Serialization is the four ROM root-hash u64 limbs, big-endian, in order —
# identical to the prover daemon's public-values prefix and the server's
# `prover_api.zisk_program_vk` expectation.
